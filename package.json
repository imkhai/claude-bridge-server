{
  "name": "claude-bridge-server",
  "version": "1.0.0",
  "type": "module",
  "description": "Bridge server between OpenClaw agents and Claude Code CLI",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
